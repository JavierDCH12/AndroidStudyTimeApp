<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/fragmentTimer">

    <fragment
        android:id="@+id/fragmentTimer"
        android:name="com.example.androidstudytimeapp.UI.FragmentTimer"
        android:label="fragment_timer"
        tools:layout="@layout/fragment_timer" />
    <fragment
        android:id="@+id/dialogTimerSettingsFragment"
        android:name="com.example.androidstudytimeapp.DialogTimerSettingsFragment"
        android:label="dialogTimerSettingsFragment"
        tools:layout="@layout/fragment_dialog_timer_settings">
        <action
            android:id="@+id/action_dialogTimerSettingsFragment_to_fragmentTimer"
            app:destination="@id/fragmentTimer" />
    </fragment>
</navigation>